function numRegions(graph) {
    // console.log(graph)
    let visiited = new Set(); 
    const numCount = 0;
    const graphKeys = graph.keys();
    
}

function _numRegions(graph) {

}

module.exports = {
    numRegions
};